[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["cd flex&roll_pro/backend && pip install -r requirements.txt"]

[start]
cmd = "cd flex&roll_pro/backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
