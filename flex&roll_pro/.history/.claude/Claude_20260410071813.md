You are a senior product designer + senior frontend engineer.

Your task is to bring this existing frontend to production-grade visual quality in one pass.

Context:
- This is an existing real project, not a greenfield redesign.
- You must significantly improve the design without rewriting architecture.
- You may refactor shared UI primitives, CSS tokens, layout wrappers, and page-level presentation.
- You must preserve the current brand palette and product structure.
- I have no time for manual cleanup, so you must do a full design pass yourself.

Goals:
- Audit the entire frontend and find all visual inconsistencies.
- Fix spacing, padding, margins, typography hierarchy, radii, borders, shadows, surface colors, hover states, density, and responsiveness.
- Replace inconsistent card styles with one coherent card system.
- Make all screens feel like one product.
- Improve design quality noticeably, not cosmetically.

Constraints:
- Do not change routes, business logic, API contracts, or app architecture.
- Do not introduce a different color identity.
- Do not leave old and new styles mixed together.
- Prefer shared reusable primitives over page-by-page hacks.

Workflow you must follow:
1. Audit all pages and shared UI components.
2. Define or refine the global visual system first:
   - spacing scale
   - card styles
   - surface system
   - border/radius system
   - buttons
   - inputs
   - badges
   - side panels
   - table/list rows
3. Apply the new system across the entire app.
4. Review every route again and remove leftovers from the old style.
5. Run build and fix all issues.
6. Before finishing, do a self-critique:
   - identify any screens that still feel weaker
   - improve them too before stopping

Quality bar:
- premium B2B SaaS
- consistent, clean, balanced, modern
- no random spacing
- no accidental colors
- no weak cards
- no visually noisy sections
- no oversized components unless intentional

Final output:
- implement the changes directly
- then give me:
  1. what you changed
  2. which shared primitives/tokens were updated
  3. any remaining imperfections


# Frontend Polish Rules

## Non-negotiables
- Do not rewrite architecture.
- Do not change routing, data flow, backend contracts, or business logic.
- Preserve the current color family and brand palette. You may rebalance tones, contrast, surfaces, shadows, and neutrals, but do not replace the core palette.
- Content may stay the same; design system must improve.

## Design goals
- Make the UI feel premium, intentional, and consistent.
- Fix spacing, alignment, hierarchy, radii, shadows, borders, hover states, empty states, table/list density, and mobile responsiveness.
- Eliminate inconsistent card styles, incorrect colors, weak contrast, cramped areas, and random component sizing.
- Unify all cards, buttons, inputs, badges, tabs, side panels, tables, and section wrappers into one system.

## Implementation rules
- Start with shared tokens and reusable UI primitives first.
- Then update all pages using the new primitives.
- Avoid generic AI-looking design.
- Keep the code clean and maintainable.
- Run build after changes and fix all errors.
- Review every route before stopping.

## Deliverables
- Updated frontend
- Short summary of changes
- List of remaining weak spots, if any
